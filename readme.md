# HTTP Microservice with Rate Limiting

A lightweight Python microservice that provides a simple HTTP API, a token bucket rate limiter, logging middleware and in-memory metrics. It’s designed to be minimal, clear and easy to extend during a coding challenge or backend practice session.

## Features
- Token bucket rate limiter with per-client tracking  
- Logging middleware for requests and responses  
- In-memory metrics for request counts and rate-limit hits  
- Simple HTTP endpoints for testing and inspection  

## Getting Started

### Install dependencies
```bash
pip install -r requirements.txt
